#pragma once

/* SETTINGS */
//screensize
const int _X = 800;
const int _Y = 600;

const float playermass = 10.0f;
const float stepsize = 20.f;


//max angular velocity of weapon rotation
const float max_rot_factor = 61.f;

// swing acceleration factor
const float swing_factor = 40.f;

const float pick_range = 30.f;

const float pick_offset = -23.f;
